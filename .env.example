# Astra AI Content OS — Backend configuration template
# Copy to .env and adjust values. Never commit .env to version control.

APP_NAME=Astra AI Content OS
APP_VERSION=0.1.0
ENVIRONMENT=local
DEBUG=true
HOST=0.0.0.0
PORT=8000
